{
  "scripts": {
    "prettier": "prettier --write 'src/*.sol' 'test/*.sol' 'src/**/*.sol'",
    "prettier-check": "prettier --write 'src/*.sol' 'test/*.sol' 'src/**/*.sol'"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.5.1",
    "@openzeppelin/contracts": "4.4.0",
    "hardhat": "^2.9.3"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
